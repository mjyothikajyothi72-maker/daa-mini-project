# daa-mini-project
This project implements a hospital priority system using the Heap Sort algorithm (Max Heap). Patients are assigned priority values, and the system sorts them in descending order so that higher-priority (more critical) patients are treated first. It demonstrates the use of data structures and algorithms in real-world hospital emergency management.
